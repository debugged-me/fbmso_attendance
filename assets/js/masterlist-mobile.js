/* Legacy compatibility shim.
   App-wide table labels, redraw observers, and card/scroller selection now
   live in assets/js/mobile-shell.js. This file remains wired temporarily so
   older cached view markup does not request a missing asset. */
(function () {
  'use strict';
})();
